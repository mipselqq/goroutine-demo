# Optional: override API origin (absolute URL, non-localhost). If unset, invalid, localhost,
# or a dev-time relative path like /api (Vite proxy), requests use https://goroutine.mipselqq.uk.
# VITE_API_BASE_URL=https://goroutine.mipselqq.uk
